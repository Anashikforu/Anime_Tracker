<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Anime Tracker</title>
		<link rel="stylesheet" href="http://localhost:8080/static/styles.css" />
	</head>
	<body>
		<header>
			<h1>My Anime List</h1>
			<a href="/">View All Anime</a>
			<a href="/add-anime">Add New Anime &#x27A2;</a>
		</header>


        <main>
            <div class="container-anime">
                <div><h1>Anime</h1></div>
                <div class="anime-content">
                    <div><img src="static/<%= anime.image %>" alt="<%= anime.name %>" /></div>
                    <div>
                        <h2><%= anime.name %></h2>

                        <p class="anime-description"><%= anime.description %></p>
                    </div>
                </div>
                <div class="anime-edit-delete">
                    <div>
                        <form method="POST" action="/delete">
                            <div>
                                <input type="hidden" value="<%= anime.id %>" name="animeId" />
                                <button>Delete</button>
                            </div>
                        </form>
                    </div>
                    <div>
                        <a href="/edit-anime/<%= anime.id %>?edit=true">Edit</a>
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>